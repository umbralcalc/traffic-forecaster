# Build artifacts
/bin/
*.test
# Stray `go build ./cmd/X` binaries land at the repo root named after the command
/build-accident-burden
/safety-backtest
/grid-sweep
/forecast
/resolve

# Local environment
.env

# Collision panel derived from DfT STATS19 (external public data) — regenerated by
# cmd/build-accident-burden, cited in SOURCES.md, never committed.
/data/incidents/

# Dead local data: the removed TfL snapshot collection (data/raw) and works
# pipeline (data/burden, data/streetmanager ~540MB). Never commit; safe to delete.
/data/raw/
/data/burden/
/data/streetmanager/
