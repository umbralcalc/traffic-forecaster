# Build artifacts
/bin/
*.test

# Local environment
.env

# Collision panel derived from DfT STATS19 (external public data) — regenerated by
# cmd/build-accident-burden, cited in SOURCES.md, never committed.
/data/incidents/

# Legacy local data from the removed works pipeline — never commit. Safe to delete
# from disk to reclaim space (data/streetmanager is ~540MB).
/data/burden/
/data/streetmanager/
