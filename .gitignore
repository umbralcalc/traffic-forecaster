# Build artifacts
/bin/
*.test

# Local secrets — the TfL app_key belongs in a repo secret / shell env, never committed
.env

# Street Manager data is a large public archive — we cite the source, never commit copies
/data/streetmanager/

# Burden series is derived from Street Manager (external) data — regenerated, not committed
/data/burden/

# Transient sweep log
/sweep.log

# Accident burden derived from DfT STATS19 (external) — cite, do not commit
/data/incidents/
